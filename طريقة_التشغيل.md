# طريقة تشغيل مشروع TavrixMapTiles

هذا المشروع يبني ملفات خرائط بصيغة PMTiles من قاعدة بيانات PostGIS، وبعدها يشغل API بسيط حتى تجرب الخريطة محليا.

## المتطلبات

- Docker Desktop لازم يكون منصب ومشتغل.
- قاعدة بيانات PostgreSQL/PostGIS لازم تكون جاهزة وبداخلها بيانات OSM من `osm2pgsql`.
- الجداول المطلوبة غالبا هي:
  - `public.planet_osm_point`
  - `public.planet_osm_line`
  - `public.planet_osm_polygon`
  - `public.planet_osm_roads`
- افتح PowerShell داخل مجلد المشروع:

```powershell
cd C:\TavrixMap\TavrixMapTiles
```

## أول تشغيل

إذا ملف `.env` غير موجود، انسخه من المثال:

```powershell
Copy-Item .env.example .env
```

افتح ملف `.env` وعدل معلومات قاعدة البيانات:

```env
POSTGIS_HOST=host.docker.internal
POSTGIS_PORT=5432
POSTGIS_DB=gis
POSTGIS_USER=pmtiles_builder
POSTGIS_PASSWORD=كلمة_المرور
REGION=iraq
S3_BUCKET=
```

ملاحظة: إذا قاعدة البيانات على نفس الجهاز، غالبا `POSTGIS_HOST=host.docker.internal` صحيح لأن التشغيل يصير من داخل Docker.

## بناء صورة Docker

نفذ هذا الأمر مرة واحدة، أو كلما تغير `Dockerfile`:

```powershell
docker compose build pmtiles-builder map-api
```

## بناء ملفات الخريطة محليا

شغل هذه الأوامر بالترتيب. خيار `--skip-upload` يعني لا ترفع الملفات إلى S3 أو MinIO، فقط خليها داخل مجلد `output`.

```powershell
docker compose run --rm pmtiles-builder python3 scripts/build.py --target global --region global --skip-upload --no-prune
```

```powershell
docker compose run --rm pmtiles-builder python3 scripts/build.py --target basemap --region iraq --skip-upload --no-prune
```

```powershell
docker compose run --rm pmtiles-builder python3 scripts/build.py --target pois --region iraq --skip-upload --no-prune
```

إذا تريد منطقة غير العراق، بدل `iraq` إلى واحدة من هذه:

```text
saudi
uae
iraq
```

الملفات الناتجة تظهر هنا:

```text
output/tiles/global/
output/tiles/iraq/
output/manifests/
```

## تشغيل السيرفر المحلي وتجربة الخريطة

بعد نجاح البناء، شغل السيرفرات:

```powershell
docker compose --profile static up -d static map-api
```

افتح هذه الروابط بالمتصفح:

```text
http://localhost:8090/demo
http://localhost:8090/docs
http://localhost:8090/api/health
http://localhost:8090/api/regions
http://localhost:8090/api/style/iraq.json
http://localhost:8088/manifests/iraq.json
```

صفحة الديمو تستخدم هذا المسار تلقائيا:

```text
http://localhost:8090/api/style.json?lon=44.3&lat=33.3&bbox=38.79,29.06,48.62,37.39&zoom=8
```

يعني أنت ما تحتاج تختار العراق أو السعودية يدويا. حرك الخريطة، والواجهة ترسل موقع الشاشة للباك اند، والباك اند يرجع الريجن أو الريجنات المناسبة إذا ملفاتها مبنية وموجودة في `output/manifests`.

إذا تريد تشوف الستايل الداكن:

```text
http://localhost:8090/api/style/iraq.json?style=dark
```

## إيقاف التشغيل

```powershell
docker compose --profile static down
```

## تشغيل المجدول الاختياري

المجدول يشغل عمليات البناء حسب `config/schedules.json`.

```powershell
docker compose --profile scheduler up -d scheduler
```

إيقاف المجدول:

```powershell
docker compose --profile scheduler down
```

## مشاكل شائعة

إذا ظهر خطأ اتصال بقاعدة البيانات، تأكد من:

- PostgreSQL شغال.
- المنفذ `5432` مفتوح.
- اسم قاعدة البيانات واسم المستخدم وكلمة المرور صحيحة في `.env`.
- إذا قاعدة البيانات على نفس الجهاز، استخدم `host.docker.internal` بدل `localhost`.

إذا ظهرت رسالة أن جداول `planet_osm_*` غير موجودة، فهذا يعني أن بيانات OSM بعد ما مستوردة إلى PostGIS.

إذا فتحت `/demo` والخريطة فارغة، تأكد أن أوامر البناء نجحت وأن ملف manifest موجود:

```text
output/manifests/iraq.json
```

إذا غيرت `.env`، أعد تشغيل الخدمات:

```powershell
docker compose --profile static down
docker compose --profile static up -d static map-api
```
